apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-smoke
  namespace: demo
  labels:
    app: nginx-smoke
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-smoke
  template:
    metadata:
      labels:
        app: nginx-smoke
    spec:
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-smoke
  namespace: demo
spec:
  selector:
    app: nginx-smoke
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
